# Herencia simple...
#Es la capacidad de una subclase que hereda atributos y metodos de una clase padre.

# class Subclase(ClasePadre):
#print("Soy una subclase que hereda de una clase padre.")

#Definamos que atributos o metodos vamos a llamar de la clase padre con la palabra reservada super(), que directamente llama al contrusctor para inicializar o complementar comportamientos

#sobreescritura de metodos
#concepto: una subclase puede redefinir (override) un metodo heredado para adaptar o ampliar su funcionalidad

#como hacerlo: definiendo en la subclase un metodo con el mismo nombre, se puede usar super().metodo() dentro de la nueva implementacion

